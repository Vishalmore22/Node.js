export const add = (a, b) => {
    console.log("addition is ", a + b);
}

const div = (a, b) => {
    console.log("divisone", a / b);
}

export default div;

// commanjs
// module.exports = { add, div };
